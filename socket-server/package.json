{
  "name": "code-quest-socket-server",
  "version": "1.0.0",
  "description": "Optional realtime battle server for Code Quest. Deploy to any Node host (Render, Railway, Fly.io) when you want sub-100ms presence + battle events instead of Supabase Realtime.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "socket.io": "^4.7.5",
    "@supabase/supabase-js": "^2.45.0"
  }
}
